

// import 'package:cloud_firestore/cloud_firestore.dart';
// import 'package:flutter/material.dart';

// String timeStampConverter(String time){
// final int seconds =
//         int.parse(time.substring(18, 28)); // 1621176915
// final int nanoseconds =
//         int.parse(time.substring(42, time.lastIndexOf(')'))); // 276147000
// final Timestamp timeConverted = Timestamp(seconds, nanoseconds);
// debugPrint(timeConverted.toDate().toString());
//   return timeConverted.toDate().toString();
// }